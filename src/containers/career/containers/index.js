import React from 'react';
import {Container, SmallTitle, Button, Form} from '../styles';
import PropTypes from 'prop-types';
import Api from '../../../saga/api';

class Career extends React.Component {

    render() {
        return (
            <>
            </>
        )
    }
}

export default Career;