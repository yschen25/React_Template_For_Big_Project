import React, {useContext} from 'react';
import {SmallTitle, Container, Button, Form} from '../styles';
import {Store} from '../../../store/hookIndex';
import PropTypes from 'prop-types';

const Index = () => {


    return (
        <>

        </>
    )
};

export default Index;
